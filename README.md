# repo-161

Python (Poetry) test repository for FOSSA analysis.

Dependency shape: `certifi` is pinned as a direct dependency and is also required
transitively by `requests`, so it should classify as both direct and transitive.
